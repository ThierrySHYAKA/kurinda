# Kurinda backend — minimal runtime dependencies.
# main.py imports only json, pathlib (stdlib), and fastapi.
# The full ML/Jupyter environment lives in ml/requirements.txt, not here —
# the backend only serves JSON and must stay lightweight to build on Render.
fastapi==0.137.2
uvicorn[standard]==0.49.0
africastalking==2.0.2
python-dotenv==1.2.2

# Database (Neon Postgres) — persistence for SMS alert history, and later
# users/interventions. SQLModel pairs SQLAlchemy + Pydantic so table models
# double as request/response schemas. psycopg[binary] avoids needing a
# compiled Postgres client on dev machines and on Render's build image.
sqlmodel==0.0.27
psycopg[binary]==3.2.10 